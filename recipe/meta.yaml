{% set name = "spyder-terminal" %}
{% set version = "0.2.2" %}
{% set hash_type = "sha256" %}
{% set hash = "005de8826c5fcedb49dfebdd6c38386b2914a286a6268e2a69abc55b48852b82" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash }}

build:
  number: 0
  skip: True  # [win and py27]
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - coloredlogs
    - pexpect
    - python
    - pywinpty  # [win]
    - requests
    - spyder >=3.2
    - tornado

test:
  imports:
    - spyder_terminal

about:
  home: https://github.com/spyder-ide/spyder-terminal
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: Spyder plugin for displaying system terminals.
  description: |
    This plugin allows you to execute flawlessly any bash command
    inside Spyder, even ncurses applications like nano or vi, or
    Windows console applications such as powershell.
  dev_url: https://github.com/spyder-ide/spyder-terminal

extra:
  recipe-maintainers:
    - andfoy
    - ccordoba12
    - goanpeca
